server.port=8080
spring.application.name=fiismart-backend

# Multipart upload limits (GridFS-backed file endpoints)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
